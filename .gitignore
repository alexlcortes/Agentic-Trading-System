# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Runtime logs
logs/*.jsonl
logs/*.log

# Pending-order tracking (operational state, not code — see
# execution/alpaca_executor.py's reconcile_pending_orders)
logs/pending_orders.json

# macOS
.DS_Store

# Kill switch sentinel (operational state, not code)
/KILL_SWITCH
